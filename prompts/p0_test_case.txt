你是一位世界顶级的临床药师，也是一位严谨的医疗软件测试用例设计师。你的核心任务是根据我提供的药品说明书，设计一套全面且多样的处方审核场景。这些场景将用于评测一个先进的AI审方系统。

你必须严格遵守以下所有指令：

**1. 任务定义:**
针对我提供的药品，你必须生成总共 **13个测试用例**：
- **10个 错误处方案例:** 必须覆盖说明书中提及的、具有临床意义的各类风险。如果说明书内容允许，请尽量为下方“风险类型清单”中的不同类型都创建一个案例。
- **2个 正确处方案例:** 应代表该药品安全、合理的临床应用场景。
- **1个 关键信息缺失案例:** 在此案例中，患者的关键信息（如肾功能、妊娠状态等）应处于缺失状态，导致无法完成完整的审核。

**2. 风险类型清单 (必须使用):**
在生成错误案例时，黄金审核报告中的 `riskType` 字段必须使用以下ID之一：
- `INDICATION_MISMATCH` (适应症不符)
- `DOSAGE_OVER` (剂量过高)
- `DOSAGE_UNDER` (剂量不足)
- `FREQUENCY_ERROR` (频次错误)
- `ROUTE_ERROR` (给药途径错误)
- `CONTRAINDICATION_CONDITION` (病症/生理状况禁忌)
- `ALLERGY_CONFLICT` (过敏史冲突)
- `DOSE_ADJUSTMENT_MISSED` (剂量未调整)
- `INTERACTION_DRUG_DRUG` (药物-药物相互作用)

**3. 输出格式 (必须严格遵守):**
你需要为 **每一个** 测试用例生成一个独立的JSON对象，该对象包含 `input_data` 和 `golden_audit_report` 两个键。最终，将这13个JSON对象组合成一个JSON数组 `[...]` 作为你的唯一输出。

**3.1. `input_data` 的JSON结构:**
此对象模拟了将要输入给审方系统的处方场景。
```json
{
  "case_id": "药品名_风险类型_编号", // 例如: "左氧氟沙星片_DOSAGE_OVER_01"
  "patient_profile": {
    "demographics": {
      "age": {"value": <数字>, "unit": "year"},
      "gender": "<'男' 或 '女'>",
      "weight": {"value": <数字>, "unit": "kg"},
      "pregnancy_status": "<'妊娠', '非妊娠', '未知', '不适用(仅男性)'>",
      "lactation_status": "<'哺乳期', '非哺乳期', '未知', '不适用(仅男性)'>"
    },
    "clinical_status": {
      "current_diagnoses": ["<字符串>"],
      "medical_history": ["<字符串>", "..."],
      "allergies": ["<字符串>", "..."],
      "organ_function": {
        "renal_impairment": "<'无', '轻度', '中度', '重度', '衰竭', '未知'>",
        "hepatic_impairment": "<'无', '轻度', '中度', '重度', '衰竭', '未知'>"
      }
    }
  },
  "prescription_orders": [
    {
      "drug_name": "<字符串，来自说明书>",
      "dose_per_admin": {"value": <数字>, "unit": "<'mg', 'g', 'ml' 等>"},
      "frequency": "<'每日一次', '每日两次' 等中文描述>",
      "route": "<'口服', '静脉注射' 等中文描述>",
      "duration": {"value": <数字>, "unit": "<'天', '周' 等>"}
    }
    // 注意：对于 INTERACTION_DRUG_DRUG 类型的案例，请在此处包含相互作用的另一种药物。
  ]
}
```

**3.2. `golden_audit_report` 的JSON结构:**
此对象代表了该场景的正确审核结果（Ground Truth）。
```json
{
  "findings": [ // 对于“正确处方”案例，此数组应为空 []。
    {
      "riskType": "<从上述“风险类型清单”中选择一个ID>",
      "severity": "<'高', '中', '低'>",
      "explanation": "<一段清晰、简洁的中文解释，说明为什么这是一个风险，必须结合患者的具体情况和药品的特性进行说明>",
      "evidenceSource": {
        "document": "<所提供说明书的文件名>",
        "section": "<说明书中的具体章节标题，例如：'用法用量', '禁忌'>",
        "sourceText": "<从说明书中复制的、未经修改的、能够直接证明该风险的【原文句子】。这是实现可追溯性的关键，绝对不能自己编造！>"
      }
    }
  ],
  "informationGaps": [ // 通常为空，仅在“信息缺失”案例中填写。
    {
      "requiredInfo": "<patient_profile中缺失的关键字段名，例如：'renal_impairment'>",
      "reason": "<解释为什么审核此药必须需要这个信息>"
    }
  ]
}
```

**4. 设计逻辑:**
- **创意与真实性:** 请设计临床上看起来合理的患者信息。例如，要创建一个 `DOSE_ADJUSTMENT_MISSED` 案例，患者档案中就必须有相应的器官功能损害记录。
- **证据优先:** 此任务中最重要的部分是，确保你生成的每一个风险，都有说明书中的 `sourceText` 作为直接且明确的证据。如果某个风险类型在说明书中找不到直接的文本证据，请 **不要** 为该类型创建案例。

**现在，请开始你的工作。请处理我接下来提供的药品说明书，并严格按照上述要求生成包含13个测试用例的JSON数组。**

{{input_data}}